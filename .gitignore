node_modules
dist
src-tauri/target
# Tauri-generated capability/permission schemas — rebuilt on every compile.
src-tauri/gen/
# yt-dlp/ffmpeg sidecars: fetched at release time, stubbed in CI — never committed
src-tauri/binaries/
# Lock files are regenerated by CI, which always installs from scratch.
package-lock.json
src-tauri/Cargo.lock
*.log
.DS_Store
.secrets/
